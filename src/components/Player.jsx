// You can enhance this later, but for now we represent player inside Maze.js directly.
// Keep this file if you want to separate player logic/rendering.

const Player = () => {
  return <div className="w-5 h-5 bg-blue-500"></div>;
};

export default Player;
